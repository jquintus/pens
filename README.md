# Pens

This repo generates parametric CAD models from YAML configs.

## Structure

- `projects/` – individual models + configs
- `cad/` – shared CadQuery logic
- `scripts/` – build orchestration
- `outputs/` – generated files

## Getting started (Dev Container)

1. Install Docker + VS Code
2. Open repo in VS Code
3. "Reopen in Container"
4. Run:

```bash
python scripts/build.py
```

## Planned Structure
```
repo/
│
├── projects/
│   ├── pen_v1/
│   │   ├── config.json
│   │   └── model.py
│   │
│   ├── pen_v2/
│   │   ├── config.json
│   │   └── model.py
│
├── cad/
│   ├── engine.py            # shared CadQuery helpers
│   └── utils.py
│
├── slicer/
│   ├── prusaslicer.ini     # locked print profile
│
├── outputs/                # generated files (optional commit)
│   ├── pen_v1.stl
│   ├── pen_v1.gcode
│
├── site/
│   ├── index.html          # optional gallery UI
│   ├── app.js
│   └── data.json           # generated by CI
│
├── scripts/
│   └── build.py            # orchestrates generation locally/CI
│
├── .github/
│   └── workflows/
│       └── build.yml       # main CI pipeline
│
└── README.md
```