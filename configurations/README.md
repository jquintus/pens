# Configurations

YAML files in this folder drive both **CadQuery builds** (`scripts/build.py`) and the **GitHub Pages gallery** (`scripts/generate_site_data.py`).

Edit them in the repo, or use [Pages CMS](https://pagescms.org) with the root `.pages.yml` (see the main README).
